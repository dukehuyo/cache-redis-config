name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Echo project name
        run: echo "Running workflow for cache-redis-config"
      - name: Simple build step
        run: echo "Build completed"